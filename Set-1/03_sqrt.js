// program to find the square root

// to find the square root of a number in javascript, can use the built-in Math.sqrt method
// -> Math.sqrt(number)

const number = 9;

// take input form user
// const number = parseInt(prompt("Enter the number: "))

const result = Math.sqrt(number);
console.log(`The square root of ${number} is ${Math.round(result)}`);
